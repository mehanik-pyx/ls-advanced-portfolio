.socials {
  display: flex;
  align-items: center;
}

.socials__item {
  margin-right: 40px;

  &:last-child {
    margin-right: 0;
  }
}

/*.socials--round-borders{
  .socials__item {
    width: 50px;
    height: 50px;
    border: 1px solid $footer-text;
    border-radius: 50%;
  }
  .socials__link {
  }
  .socials__icon {
    fill: $footer-text;
    display: flex;
    align-items: center;
  }
}*/

.socials__icon {
  width: 29px;
  height: 29px;
  display: block;
  fill: $text-color;
}